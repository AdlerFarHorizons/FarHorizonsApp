<%= form_for(@predicted_track) do |f| %>
  <% if @predicted_track.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@predicted_track.errors.count, "error") %> prohibited this predicted_track from being saved:</h2>

      <ul>
      <% @predicted_track.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :source %><br>
    <%= f.text_field :source %>
  </div>
  <div class="field">
    <%= f.label :file_path %><br>
    <%= f.text_field :file_path %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
