<h1>Editing <%=  @object.class.to_s.titleize %></h1>
<id="notice">Message:<%= notice %><br><br>

<%= render 'common/tree_edit' %>

<%= link_to 'Show', @object %> |
<%= link_to 'Back', "/#{@object.class.to_s.pluralize.titleize.downcase.split.join('_')}" %>
